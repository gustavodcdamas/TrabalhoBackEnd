# ===========================
# CONFIGURAÇÕES PARA DESENVOLVIMENTO LOCAL
# ===========================

# ========== DOCKER COMPOSE ==========
NODE_ENV=development
BUILD_TARGET=development

# Portas e mapeamentos
FRONTEND_PORT=3535
FRONTEND_INTERNAL_PORT=4200
BACKEND_PORT_MAP=3333:3333
DB_PORT_MAP=5433:5432

# URLs
FRONTEND_URL=http://localhost:3535

# Rede
EXTERNAL_NETWORK=default

# ========== NESTJS BACKEND ==========
# Auth
JWT_SECRET=O9~m1+Q0ik123cI£Y&G?^££JcW80$h93kad£el
SESSION_SECRET=outra-chave-secreta-diferente-do-jwt
JWT_EXPIRATION_TIME=1d
BCRYPT_ROUNDS=12

# Nest
NEST_PORT=3333

# Rate Limiting
THROTTLE_TTL=60000
THROTTLE_LIMIT=100

# Upload
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp

# ========== DATABASE ==========
# Para desenvolvimento híbrido (backend docker + frontend local)
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=dev_user
DB_PASSWORD=dev_password
DB_NAME=agencia_dev

# Para Docker Compose
POSTGRES_PASSWORD=dev_password
POSTGRES_USER=dev_user
POSTGRES_DB=agencia_dev

# Database URL completa
DATABASE_URL=postgresql://dev_user:dev_password@localhost:5433/agencia_dev

# ========== REDIS ==========
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=dev_password
REDIS_TTL=86400
REDIS_URL=redis://localhost:6380

# ========== SUPER ADMIN ==========
SUPER_ADMIN_EMAIL=admin@dev.local
SUPER_ADMIN_PASSWORD=dev123456

# ========== EMAIL (Desenvolvimento - Mock) ==========
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test
SMTP_FROM=dev@localhost
FROM_NAME="Agência Dev - Não Responda"
SMTP_SECURE=false

# Configurações de Retentativa
EMAIL_MAX_RETRIES=3
EMAIL_RETRY_DELAY_MS=1000
EMAIL_SMTP_TIMEOUT=10000

# ========== WHATSAPP ==========
WHATSAPP_API_URL=http://localhost:8080
WHATSAPP_API_KEY=dev_key
WHATSAPP_SESSION_NAME=dev_session
WHATSAPP_TIMEOUT=30000

# ========== HTTP ==========
HTTP_TIMEOUT=5000
HTTP_MAX_REDIRECTS=5

# ========== API ==========
JWT_VERIFICATION_SECRET=O9~m1+Q0ik123cI£Y&G?^££JcW80$h93kad£el
MAIL_FROM=noreply@dev.local
BASE_URL=http://localhost:3333
